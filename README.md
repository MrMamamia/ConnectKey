# ConnectKey
ConnectKey.AI is a lightweight, multi-provider AI chat client built with vanilla HTML, CSS, and JavaScript. It supports streaming responses, markdown rendering, file attachments, IndexedDB chat history, customizable system prompts, web search integration, and a clean, responsive interface.
